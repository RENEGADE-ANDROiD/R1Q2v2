#define BUILD "8051"
/* BUILD 8051:
 * cl_maxpackets (default 60) on protocol 34/35 via 2-cmd recovery.
 * Instant send on attack/use/jump edges. HTTP after read+send in sync.
 * BUILD 8050: ICMP skip, SIO_UDP_CONNRESET off, CMD_BACKUP 128.
 */
